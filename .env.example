# Puertos publicados por Docker Compose. Los valores por defecto permiten usar
# `docker compose up -d --build --wait` sin copiar este archivo.
KANBAI_DB_PORT=5432
KANBAI_BACKEND_PORT=8000
KANBAI_FRONTEND_PORT=5173
